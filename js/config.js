/**
 * config.js
 *
 * Configurações globais do frontend edge-monitor.
 * Atua como fonte única de verdade (SSOT) para:
 * - URL base da API
 * - Chaves de armazenamento local
 */

const API_CONFIG = {
    BASE_URL: "http://localhost:8000",
    TOKEN_KEY: "edge_monitor_token",
    USER_KEY: "edge_monitor_user"
};

